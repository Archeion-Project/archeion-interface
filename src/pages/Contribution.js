import React from 'react';
import ReactDOM from 'react-dom';

const Contribution = () => {

	const element = 'Minhas transcrições';

	return (
		ReactDOM.render(element, document.getElementById('appView'))
	);
}

export default Contribution;