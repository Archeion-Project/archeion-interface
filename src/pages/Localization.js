import React from 'react';
import ReactDOM from 'react-dom';

const Localization = () => {

	const element = 'Localização';

	return (
		ReactDOM.render(element, document.getElementById('appView'))
	);
}

export default Localization;