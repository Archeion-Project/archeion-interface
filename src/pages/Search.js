import React from 'react';
import ReactDOM from 'react-dom';

const Search = () => {

	const element = 'Busca no Acervo';

	return (
		ReactDOM.render(element, document.getElementById('appView'))
	);
}

export default Search;