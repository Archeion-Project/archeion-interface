import React from 'react';
import ReactDOM from 'react-dom';

const Funds = () => {

	const element = 'Acervo Histórico';

	return (
		ReactDOM.render(element, document.getElementById('appView'))
	);
}

export default Funds;