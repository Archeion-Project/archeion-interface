import React from 'react';
import ReactDOM from 'react-dom';

const Contact = () => {

	const element = 'Contato';

	return (
		ReactDOM.render(element, document.getElementById('appView'))
	);
}

export default Contact;